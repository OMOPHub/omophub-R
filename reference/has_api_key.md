# Check if API Key is Available

Checks whether an OMOPHub API key is available without throwing an
error.

## Usage

``` r
has_api_key()
```

## Value

`TRUE` if an API key is available, `FALSE` otherwise.
